/*
 * KanjiPage Messages
 *
 * This contains all the text for the KanjiPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.KanjiPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the KanjiPage container!',
  },
});
